# Azure (optional — skip if only using GCP)
AZURE_TENANT_ID=
AZURE_CLIENT_ID=
AZURE_CLIENT_SECRET=
AZURE_SUBSCRIPTION_ID=

# GCP (optional — skip if only using Azure)
GCP_PROJECT_ID=
GCP_BILLING_TABLE=
GOOGLE_APPLICATION_CREDENTIALS=

# Server config
CACHE_TTL_SECONDS=300
LOG_LEVEL=info
